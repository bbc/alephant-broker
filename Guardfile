guard "rspec" do
  watch(/^spec\/.+_spec\.rb$/)
  watch(/^lib\/.+\.rb$/)
  watch(/^spec\/support\/(.+)\.rb$/)                  { "spec" }
  watch("spec/spec_helper.rb")                       { "spec" }
end
